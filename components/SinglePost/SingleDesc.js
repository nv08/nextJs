import React from "react";

export default function SingleDesc({ description }) {
  return (
    <div className="single-description">
      <span className="single-desc">{description}</span>
    </div>
  );
}
